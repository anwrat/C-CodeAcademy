#include <iostream>

//For printing through terminal, go to the path in terminal and enter g++ filename.cpp
//Then it creates a file such as a.exe, type ./a.exe  or the filename that is created

int main(){
    std::cout<<"Testing terminal output";
    return 0;
}